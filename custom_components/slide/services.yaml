calibrate:
  fields:
    entity_id:
      required: true
      selector:
        entity:
          integration: slide
          domain: cover
touchgo:
  fields:
    entity_id:
      required: true
      selector:
        entity:
          integration: slide
          domain: cover
    touchgo:
      required: true
      selector:
        boolean:
